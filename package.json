{
    "name": "merge-project",
    "version": "1.0.0",
    "description": "Merge three sorted arrays without using sort()",
    "scripts": {
        "build": "tsc",
        "test": "jest",
        "test:coverage": "jest --coverage"
    },
    "devDependencies": {
        "@types/jest": "^29.5.12",
        "@types/node": "^20.0.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.4",
        "typescript": "^5.4.0"
    }
}